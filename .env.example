# Copy to .env and fill in values

# Enables the demo banner in the UI
VITE_DEMO=true

# Port for the Express server (default: 3001)
PORT=3002

# Secret header required to call POST /api/reset-demo
# Leave blank to allow unauthenticated resets (not recommended in production)
RESET_SECRET=your-secret-here
